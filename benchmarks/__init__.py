"""The benchmark: an agentic data science workflow, run end to end on a real binary classification problem.

`dataset.py` writes the data, `tools.py` holds the functions the agents call, `prompts/` holds what each
agent is told, `agentic_configurations.yaml` wires all of it together, and `run_benchmark.py` runs it.
"""
